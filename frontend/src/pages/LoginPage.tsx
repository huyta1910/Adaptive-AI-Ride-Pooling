import { PlaceholderPage } from "@/components/shared/PlaceholderPage";

export function LoginPage() {
  return (
    <PlaceholderPage
      title="Login"
      description="Authentication page shell for future account access."
    />
  );
}
